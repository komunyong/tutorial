# Command


```shell
# First time
python -m venv venv_llm

cd mlops/llm_from_scratch/venv_llm
source Scripts/activate
pip install ipykernel

# restart VSCode

# Choose ipykernel for your notebook at `venv_llm`
pip install torch
pip install tiktoken

# Everytime
cd mlops/llm_from_scratch/venv_llm
source Scripts/activate
```